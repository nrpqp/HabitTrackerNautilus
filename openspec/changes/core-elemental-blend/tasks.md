## 1. Geometría y construcción de las formas (revisado: sin cuñas de torta)

- [x] 1.1 En `src/render/svg.js`, reemplazar el grupo `<g class="core-blend">` de cuñas por un grupo con un `<circle>` difuminado por hábito guardado (mismo criterio que antes: sobre `list` completo, no filtrado a activos-hoy, para no depender de un signature de rebuild aparte); verificar que se crea una sola vez y sólo se mutan atributos en repintados posteriores (nada de `innerHTML`)
- [x] 1.2 Calcular la posición (ángulo + radio desde el centro, dentro del núcleo) de cada círculo con una semilla determinística por `habitId` (hash simple, no por índice de lista) y un radio de dispersión que garantice solape frecuente entre 2+ formas activas; verificar visualmente con 1, 4 y 7 hábitos que las posiciones no dejan formas aisladas sin solapar nunca con otra — hash FNV-1a con sal; valores finales `CORE_BLEND_RADIUS = 28` y `CORE_BLEND_SPREAD = 20`, ajustados probando en vivo (con spread 15 las manchas se apilaban todas en el mismo punto y con 24 empezaban a separarse)
- [x] 1.3 Aplicar `filter: blur(...)` y `fill` con `elementColor(habit.element, ...)` por círculo, y `mix-blend-mode: screen` (o `lighten`) al grupo — a diferencia de la iteración anterior, acá las formas SÍ se solapan a propósito, así que el blend mode debe generar un tono visiblemente nuevo en la intersección; verificar en tema claro y oscuro que el resultado se lee como mezcla y no como manchas turbias — **desvío deliberado**: en vez de `filter: blur()` se usa un `<radialGradient>` por mancha, que da el mismo borde difuso sin coste de filtro, sin recorte del área filtrada y escalando con el viewBox (misma técnica que `glowSprite` en fx/engine.js). Blend: `screen` en tema oscuro, `multiply` en claro (donde `screen` lavaba todo a blanco), verificado en ambos

## 2. Estado visible/oculto por forma

- [x] 2.1 Actualizar `setCoreBlend(doneIds)` en `src/fx/effects.js` para el nuevo grupo de círculos: por cada hábito completado hoy, opacidad visible; el resto, `opacity:0` sin ningún rastro visible (ni contorno ni sombra) — verificar inspeccionando el DOM que la forma sigue existiendo pero es indistinguible del fondo en reposo — confirmado en DOM: 7 manchas presentes, 0 encendidas, `computedOpacity: "0"`
- [x] 2.2 Transición CSS de opacidad/escala al aparecer/desaparecer, deshabilitada en `html[data-tier="1"]` y bajo `prefers-reduced-motion` (mismo patrón que antes); verificar que en nivel 1 el cambio es instantáneo y en nivel 2+ es animado — verificado `?fx=1` (`0s`) y `?fx=4` (`0.42s`)
- [x] 2.3 Estado neutro sin hábitos completados hoy: todas las formas en `opacity:0`, núcleo con su `fill` de base (`tc.centerFill`) igual que antes de este cambio, sin ningún indicio de "espacios pendientes"; verificar visualmente al cargar con cero hábitos marcados y con varios hábitos activos pero ninguno completado — verificado con 7 hábitos activos y ninguno marcado: núcleo limpio, sin rastro de casilleros

## 3. Conexión con el estado de reposo del centro

- [x] 3.1 Confirmar que `refreshDayCore()` en `src/main.js` sigue llamando a `setCoreBlend(doneIds)` (ya no llama a `setCoreLabel` con la cifra en reposo); verificar que ya no aparece ningún texto numérico en el núcleo en reposo — sin cambios necesarios; `coreText` vacío en reposo
- [x] 3.2 Re-verificar que `showStreakInCore`, la previsualización del selector radial (`showRadialAim`/`endRadialAim`) y el mensaje "sin retos" siguen funcionando igual sobre las nuevas formas — todos "toman prestado" el centro con `coreShowingTransient` y, al terminar, `refreshDayCore()` restituye la mezcla — verificado vía el gesto radial completo (preview + confirmación + restitución)
- [x] 3.3 Re-verificar que marcar y desmarcar por click directo de celda, y por el gesto radial (`radial-quick-mark`), actualizan la mezcla igual en ambos caminos — celda: 3→4 manchas y 4→3 al desmarcar; gesto radial: 3→4; en ambos la opacidad se recalcula sola

## 4. No regresión de lo que no cambia

- [x] 4.1 Verificar que el anillo de segmentos (`gauges`) sigue rellenándose/vaciándose exactamente igual que antes de este cambio — 4 `gauge-arc` con `stroke-dashoffset: 0` coincidiendo con los 4 hábitos marcados
- [x] 4.2 Verificar que `setCoreCharge` (el brillo que crece con la proporción del día) sigue aplicándose igual, en paralelo a la mezcla, y que no choca visualmente con el nuevo `filter: blur()` de las formas (ambos mutan `style.filter`/`filter` del núcleo o de sus hijos — confirmar que no se pisan) — sin choque posible: al usar `<radialGradient>` en vez de `filter`, las manchas tienen `filter: none`; el `drop-shadow` de `setCoreCharge` sigue intacto en el núcleo
- [x] 4.3 Verificar que la celebración de día completo (supernova) y el cometa de racha no cambian de comportamiento — sin cambios de código en `streakComet`/`supernova`; el día completo (7/7) se alcanzó durante las pruebas sin errores

## 5. Verificación de integración

- [x] 5.1 Probar el flujo completo en `npm run dev`: marcar y desmarcar hábitos con 1, 4 y 7 activos, en tema claro y oscuro, verificando que la mezcla se lee como formas que se agregan (no como sectores que faltan) en todos los casos — verificado con 1, 2, 3, 4 y 7; en 7 hizo falta repartir la opacidad entre las manchas encendidas (`setCoreBlend`) porque `screen` saturaba a gris lavado justo en el día completo
- [x] 5.2 Probar con `?fx=1` y `?fx=5`, y con `prefers-reduced-motion` activado, que la transición se comporta según lo definido en la tarea 2.2 — verificado `?fx=1`/`?fx=4`; `prefers-reduced-motion` no se forzó vía sistema operativo en este entorno, pero comparte el mismo `data-tier="1"` que ya se verificó
- [x] 5.3 Confirmar en las herramientas de rendimiento del navegador que el núcleo en reposo (sin marcar nada) no mantiene un `requestAnimationFrame` corriendo — `filter: blur()` en CSS no debería requerirlo, pero confirmar que no introduce un costo de repintado notable con varias formas superpuestas — `__nautilusFx.fps === 0` tras 2,5s de carga limpia con 4 manchas encendidas
- [x] 5.4 Ejecutar `npm run build` y `npm run preview`, hacer hard refresh, y confirmar que la mezcla se ve igual bajo el build cacheado por el service worker — el SW servía el bundle anterior hasta borrar cachés y desregistrarlo; con el bundle nuevo (`index-oLSt2vlR.js`) la mezcla se ve idéntica al modo dev

## 6. Iconos elementales en el núcleo (electrones)

- [x] 6.1 Añadir un `<text>` con el icono del elemento por hábito guardado, en un grupo propio por encima de las manchas y **sin** modo de fusión (el `screen`/`multiply` de las manchas lavaría el color de los emojis); verificar que el grupo se construye una sola vez y que `paint()` sólo actualiza el glifo cuando cambia el elemento del hábito
- [x] 6.2 Posicionar los iconos en una órbita común (`CORE_BLEND_ICON_ORBIT = 33`) en el mismo ángulo que su mancha, en vez de en el centro de ella; verificado con 7 hábitos: en el centro de cada mancha los emojis se encimaban hasta ser ilegibles (los centros caen dentro de un radio de 20), y en la órbita quedan a 21,7px de separación mínima
- [x] 6.3 Encender/apagar el icono junto con su mancha desde `setCoreBlend`, pero **sin** repartir su opacidad entre los encendidos como se hace con las manchas — de lo contrario el día completo mostraría siete emojis casi invisibles; verificar que con ningún hábito completado no queda ningún icono visible (`opacity: 0`, sin rastro)
- [x] 6.4 Pulso de aparición (`core-icon-pop`) al encenderse, sin animación continua en reposo, deshabilitado en `html[data-tier="1"]`; verificado: la animación se dispara al marcar, en nivel 1 no hay transición ni animación, y en reposo las únicas animaciones activas son los `today-indicator` que ya existían
- [x] 6.5 Verificar el conjunto en tema claro y oscuro, con 3 y 7 hábitos completados, y bajo el build de producción con service worker
